application.domain: http://localhost:8080

---

spring.config.activate.on-profile: dev

application.domain: ${DOMAIN}

oci:
  config: ${OCI_CONFIG_PATH}
  bucket-name: ${OCI_BUCKET_NAME}

object-storage:
  pre-authorized-url: ${OCI_PRE_AUTHORIZED_URL}